#include<stdio.h>
#include<conio.h>
int add(int n)
{
int i,sum=0;
for(i=0;i<=n;i++)
sum=sum+i;
printf("sum 0f %d id %d \n",n,sum);
retrurn sum;
}
void main()
{
int n,i,sum;
clrscr();

printf("\n enter an integer \n");
scanf("%d",&n);
add(n);
getch();
}
